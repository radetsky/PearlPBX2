.python-venv
.vscode
db.sqlite3
__pycache__
.coverage
.DS_Store
.env
*.env
staticfiles/
*.pyc
.idea/
local_settings.py

# Internal planning/task notes — not part of the public release.
# Anchored to the repo root (leading slash): an unanchored "tasks/" also
# matches every ansible/roles/*/tasks/ directory, silently dropping new
# Ansible task files from `git add`.
/tasks/
