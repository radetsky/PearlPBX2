AMI_HOST=10.35.35.1
AMI_PORT=5038
AMI_USER=dashboard_listener
AMI_PASS=QvM4atsF2PeJBOkR67Xh5miFYpepEEndYwLXhfcUzkkTE5FzDl6iT9KT8PrWuFQRJBWPMmraDlf0dR2hTDiQ
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
LOGLEVEL=10

# Slack notifications for missed queue calls (optional).
# Leave SLACK_MISSED_CALL_WEBHOOK_URL empty to disable.
SLACK_MISSED_CALL_WEBHOOK_URL=
SLACK_TIMEOUT=4
SLACK_USERNAME=PearlPBX2
# Aggregate all abandons within this window (seconds) into a single Slack message.
MISSED_CALL_DEBOUNCE_SECONDS=60

# Send call.outgoing (and its answered/ended chain) for channels Asterisk
# created via Dial()/Originate() that have not yet been Goto()'d to a real
# extension (exten is still the dialplan placeholder "s"). Off by default —
# these are system-internal channels with no dialed number to show the CRM.
WEBHOOK_SEND_SYSTEM_CHANNELS=false

# Comma-separated channel variable names to expose to CRM webhooks as
# "channel_vars" (e.g. ULINE, set by services/fastagi's parking-uline handler).
# Only variables listed here are tracked and forwarded; anything else set via
# VarSet is ignored for webhook purposes.
WEBHOOK_CHANNEL_VARS=ULINE

